services:
  - type: web
    name: vinmecprep-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: REDIS_URL
        fromService:
          type: redis
          name: vinmecprep-redis
          property: connectionString
      - key: AGENT_API_KEY
        sync: false
      - key: TRAINER_API_KEY
        sync: false

  - type: redis
    name: vinmecprep-redis
    plan: free
    ipAllowList: []
    maxmemoryPolicy: allkeys-lru